# Ablation profiles

Place ablation profile directories here, one per ablation, following the same
layout as `../phase2/<variant>/` (a `profile.yaml` manifest + the config files
it references). Existing A1–A4 scaling-experiment overrides still live in
`drl_agent/config/experiments/A1..A4/` (legacy path, unchanged).
