# Default configuration for dataset runs
dataset_root: /root/drl_path_final/ros2_ws/data
use_sim_time: true
segment_duration_sec: 600

# Base policy name only; run_manager will expand to:
#   "time_based_<segment_duration_sec>s"
split_policy: "time_based"

fixed_frame: odom
base_frame: base_link
pose_source: odom